import React from 'react';
// @ts-ignore
// import { Button } from '@shared-ui';

const Profile: React.FC = () => {
  return (
    <div>
      <h2>Profile Microfrontend</h2>
      {/* <Button onClick={() => alert('Profile Button Clicked!')}>Profile Action</Button> */}
    </div>
  );
};

export default Profile;
